# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Generated data (regenerated on build, but keep them committed so the
# repo is buildable without running the data script first — uncomment to
# ignore if you prefer regenerating on every build).
# src/data/posts.json
# src/data/photos.json

# Vite cache
.vite/

# Env
.env
.env.local
.env.*.local
